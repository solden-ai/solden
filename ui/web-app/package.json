{
  "name": "clearledgr-web-app",
  "version": "0.1.0",
  "description": "workspace.soldenai.com — coordination layer hub. Renders the Box system of record; Gmail/Slack/Teams/ERP are companion surfaces.",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "start": "node server.js",
    "start:prod": "npm run build && node server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "express": "^4.21.2",
    "htm": "^3.1.1",
    "http-proxy-middleware": "^3.0.5",
    "preact": "^10.29.0",
    "wouter-preact": "^3.7.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.0",
    "@testing-library/preact": "^3.2.4",
    "happy-dom": "^20.8.9",
    "vite": "^8.0.13",
    "vitest": "^3.2.4"
  }
}
